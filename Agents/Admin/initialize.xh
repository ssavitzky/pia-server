<!DOCTYPE  html PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN">
<html><head>
<title>Initialize PIA</title>
</head><body bgcolor='#ffffff' link='#c40026' vlink='#e40026'>
<h1>The &AGENT:name; agent initialization document</h1>

<user-message>Initializing &AGENT:name; from &filePath;</user-message>

<if><status src="START-UP.html" item="exists" />
    <then>
	<user-message>Submitting forms in '<status src="START-UP.html"
				            item='path' />'</user-message>
    </then>
    <else-if><status src='agent-files.xml' item='exists' />
    <then><user-message>Loading XML from '<status src='agent-files.xml'
				           item='path' />'</user-message>
	  <load-agent-files><include src='agent-files.xml' /></load-agent-files>
    </then></else-if>
    <else>
	  <user-message>Starting default DOFS and Demo agents.</user-message> 
	  <agent-files>./DOFS/AGENTS.xml ./Demo/AGENT.xml</agent-files>
    </else>
</if>

<hr />
<p> This form gets requested by the Admin agent at start-up.  It tries the
    following initialization techniques, stopping on the first one that works:
</p>
<ol>
  <li> Submitting all the forms in (legacy) <code>START-UP.html</code>
  <li> Loading XML from <code>load-agents.inc</code>
  <li> Starting the default DOFS and Demo agents.
</ol>

<p> It is also possible to override <em>this</em> file with your own version,
    in (<code>/~/Agents/Admin/initialize.xh</code>) and put arbitrary code
    into it. </p>

<short-footer cvsid='$Id: initialize.xh,v 1.8 1999-05-18 20:12:54 steve Exp $' />
</p></body></html>
